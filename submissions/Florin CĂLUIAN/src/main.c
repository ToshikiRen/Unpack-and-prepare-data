#include "tree.h"
#include "utils.h"

int main()
{
	//int N;
	//FILE* f=fopen("../data/test-createbalanced.txt",READ_MODE);
	//fscanf(f,"%d",&N);
	//TreeNode *rad=createBalanced(N,f),*L,*arr;
	//inorderIterative(rad,&arr,N);
	//L=LCA(rad,arr+5,arr+7);
	//printf("\n%d",L->val);
	// Daca testati o anumita functie in main, pentru a afla numarul de noduri
	// dintr-un arbore, folositi functia getNumberOfNodes din fisierul utils.h.
	return 0;
}
